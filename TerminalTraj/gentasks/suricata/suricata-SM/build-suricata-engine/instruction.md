Onboard this Suricata checkout at `/app` into a compiled IDS binary on the local toolchain. The tree ships source and autotools inputs only; you must produce a working engine binary from it.

From `/app`, regenerate the build system if needed, configure with prefix `/usr/local`, and build so the main engine binary is available at `/app/src/suricata`. That binary must run `/app/src/suricata -V` successfully and print a version line containing the substring `Suricata`. Create a minimal but valid runtime configuration at `/results/suricata.yaml` that the binary accepts, and a log directory at `/results/suricata-log`. Running `/app/src/suricata -T -c /results/suricata.yaml -l /results/suricata-log` must exit 0 (configuration test only). Do not modify files under `/data`. You may adjust build-system files under `/app` if the stock tree fails to compile in this environment, but the resulting binary must still be Suricata built from this source.

Write `/results/build-info.json` as a single JSON object with exactly these keys: `binary_path` (string, absolute path to the engine binary), `version_line` (string, the full first line from `-V`), `configure_args` (string, the configure command line you used after the configure script path), `config_test_exit` (integer, exit code of the `-T` run, must be 0), `binary_bytes` (integer, size of the binary file in bytes, must be > 0). End with a trailing newline. Also copy the final `/results/suricata.yaml` you tested and leave `/results/suricata-log` in place from the successful `-T` run.
