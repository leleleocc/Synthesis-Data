Migrate the Redis build in `/app/src/Makefile` to a profiling-friendly `-O2` compile that still keeps the Linux jemalloc vs libc zmalloc invariant. Change the default `OPTIMIZATION` to `-O2` and ensure Redis objects are compiled with `-fno-omit-frame-pointer` via the Redis-only flag channel (not by forcing those flags into dependency builds). Do not switch the Linux allocator to libc, do not set `USE_JEMALLOC=no`, and do not enable `SANITIZER` (address/undefined sanitizers force libc). Leave `zmalloc.c` / `zmalloc.h` allocator selection intact.

Compile with `gcc` by running `make` from `/app` so `/app/src/redis-server` is a newly linked ELF executable. Do not modify files under `/src` or `/data`.

Write `/results/build-report.json` as UTF-8 JSON with a trailing newline and exactly these keys in this order: `optimization` (string; the Makefile `OPTIMIZATION` value now in force), `frame_pointer_flag` (string; `-fno-omit-frame-pointer`), `malloc` (string; `jemalloc` or `libc` as actually selected on this Linux host), `use_jemalloc_macro` (boolean; whether the server objects were compiled with `USE_JEMALLOC`), `binary` (string; `/app/src/redis-server`). After the build, `/app/src/redis-server` must start, report a jemalloc `mem_allocator` in `INFO memory`, and then exit cleanly on `SHUTDOWN`; leave no server process running.
