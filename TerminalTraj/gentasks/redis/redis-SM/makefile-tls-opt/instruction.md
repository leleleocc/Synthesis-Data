Migrate `/app/src/Makefile` so the default `make` at `/app` compiles Redis with `-O2` and without LTO, while the optional TLS build path stays intact. `make BUILD_TLS=yes` must still define `USE_OPENSSL` and link `libhiredis_ssl`, `make BUILD_TLS=module` must still produce `/app/src/redis-tls.so`, and `make OPTIMIZATION=-O3` must still enable LTO exactly when optimization is `-O3`. Persist-settings must keep recording `BUILD_TLS` so a later `make` does not silently drop TLS. Do not remove jemalloc on Linux.

Leave these artifacts: default-optimized `/app/src/redis-server` and `/app/src/redis-cli` from `make` with no extra knobs; a TLS-builtin server copied to `/results/redis-server-tls`; and `/results/redis-tls.so` from the module path. `/results/build_flags.env` must be ASCII `KEY=value` lines with a trailing newline and keys in this order: `DEFAULT_OPTIMIZATION`, `DEFAULT_HAS_FLTO`, `DEFAULT_MALLOC`, `TLS_BUILTIN_HAS_OPENSSL`, `TLS_MODULE_EXISTS`, `O3_HAS_FLTO`. `DEFAULT_OPTIMIZATION` must be `-O2`. `DEFAULT_HAS_FLTO` must be `0` (default objects built without `-flto`) and `O3_HAS_FLTO` must be `1` (`OPTIMIZATION=-O3` objects built with `-flto`). `DEFAULT_MALLOC` must be `jemalloc`. `TLS_BUILTIN_HAS_OPENSSL` must be `1`: `/results/redis-server-tls --version` must run and that binary must be linked against OpenSSL. `TLS_MODULE_EXISTS` must be `1`: `/results/redis-tls.so` must be a non-empty ELF shared object.

Default `/app/src/redis-server --version` must still run. Do not require network, `make test`, or certificates.
