Repair the AOF persistence path in `/app/src/aof.c` so that rewriting the AOF fsync policy still preserves the replica offset used for WAITAOF acknowledgements. An incident in this tree dropped that offset on background AOF fsync jobs and on the synchronous always-fsync path, so durability waits no longer track the master's replication offset across `always`, `everysec`, and `no`. Restore the offset handoff for BIO fsync/close jobs, for the always path that publishes a fsynced replica offset, and for the AOF rewrite path that seeds the pending offset before the new INCR file is used.

Rebuild from `/app` with `make` so `/app/src/redis-server` is a newly linked ELF executable built from the repaired sources. Keep `/app/src/aof.c` as the repaired source of truth. Do not modify files under `/src` or `/data`, and do not paper over the invariant by only changing `/app/redis.conf`.

After the rebuild, a server started with appendonly enabled must still advance the fsynced replica offset when appendfsync is `always`, and background fsync jobs must record the offset they actually synced, so WAITAOF local and replica copies can complete after fsync-policy changes instead of blocking until timeout or acknowledging too early.
