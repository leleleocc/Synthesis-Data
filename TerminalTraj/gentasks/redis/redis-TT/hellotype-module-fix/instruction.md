The in-tree example module at `/app/src/modules/hellotype.c` is going into an internal cache service. Keep the existing `HELLOTYPE.INSERT`, `HELLOTYPE.LEN`, and `HELLOTYPE.BRANGE` commands working, including RDB load/save and AOF rewrite for the `hellotype` native type. Fix `HELLOTYPE.RANGE` so that the `first` argument is a 0-based skip count (it must not always start at the head) and `count` is the maximum number of integers returned. Empty keys still return an empty array; negative `first` or `count` still return an error.

Add two write/read commands on the same type. `HELLOTYPE.SUM key` returns the int64 sum of every stored value, or `0` when the key is missing or empty. `HELLOTYPE.REMOVE key value` deletes one matching integer (the first node with that value), returns `1` if a node was removed and `0` otherwise, and must persist: after `HELLOTYPE.REMOVE`, both an RDB `SAVE`/`DEBUG RELOAD` cycle and an AOF rewrite plus process restart must restore the post-remove list. `HELLOTYPE.INSERT` must keep the list ordered. Wrong-type keys (for example a Redis string) must still return the module wrong-type error for RANGE, LEN, SUM, and REMOVE.

Build `/app/src/redis-server` from `/app` and build `hellotype.so` with the module Makefile under `/app/src/modules`. Write `/results/hellotype.conf` with `bind 127.0.0.1`, `port 6379`, `protected-mode yes`, `daemonize no`, `dir /results/hellotype`, `dbfilename dump.rdb`, `appendonly yes`, `appendfilename appendonly.aof`, `appenddirname appendonlydir`, `loadmodule /app/src/modules/hellotype.so`, `enable-debug-command yes`, `pidfile /results/hellotype.pid`, and `logfile /results/hellotype.log`. Create `/results/hellotype` and start the server with that config; leave it running on `127.0.0.1:6379`.

Write `/results/hellotype-report.json` as a single UTF-8 JSON object with no BOM, Unix newlines, and a trailing newline. Use exactly these keys in this order: `module_loaded`, `range_skips_first`, `sum_empty`, `remove_persists`. Set every value to boolean `true` only if you have actually verified that behavior against the running server (module appears in `MODULE LIST`, `RANGE` with `first=1` skips the head, `SUM` of a missing key is `0`, and `REMOVE` survives `SAVE` plus `DEBUG RELOAD`).
