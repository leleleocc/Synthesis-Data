Harden a local Redis 7.2.16 instance so application traffic never uses TCP and the default user cannot run commands. Build `/app/src/redis-server` and `/app/src/redis-cli` from `/app`. A rejected ACL draft lives at `/data/acl/users.acl`; do not modify `/data`. Produce the live files at `/results/redis.conf` and `/results/users.acl`.

`/results/redis.conf` must set `bind 127.0.0.1`, `port 0`, `protected-mode yes`, `unixsocket /results/redis.sock`, `unixsocketperm 700`, `aclfile /results/users.acl`, `daemonize no`, `dir /results/redis-data`, `dbfilename dump.rdb`, `pidfile /results/redis.pid`, `logfile /results/redis.log`, `save ""`, and `appendonly no`. Do not set `requirepass` (it is incompatible with `aclfile`). Create `/results/redis-data` and start the server with that config; leave it running. Nothing may accept connections on TCP port 6379. The Unix socket `/results/redis.sock` must exist with mode `0700`.

`/results/users.acl` must define exactly three users using Redis ACL file syntax, one `user` directive per line, Unix newlines, trailing newline: `default` is `off` with `-@all` and no key pattern that grants writes; `app` is `on` with password `app-secret-7`, command categories `+@connection +@read +@write +@string +@hash +@list -@admin -@dangerous`, key pattern `app:*` only, and `resetchannels`; `ops` is `on` with password `ops-secret-7`, `+@all`, `~*`, and `allchannels`. Unauthenticated connections on the socket must fail. The `app` user must `PING` and `SET app:health 1` successfully, and must receive an ACL error for `SET other:health 1`. The `ops` user must be able to run `ACL WHOAMI` and receive `ops`.

Write `/results/acl-report.json` as a single UTF-8 JSON object with no BOM, Unix newlines, and a trailing newline. Use exactly these keys in this order: `tcp_port`, `unixsocket`, `default_off`, `app_ping`, `app_wrong_key`, `ops_whoami`. Set `tcp_port` to the integer `0`, `unixsocket` to the string `/results/redis.sock`, `default_off` to boolean `true`, `app_ping` to the string `PONG`, `app_wrong_key` to the string `denied`, and `ops_whoami` to the string `ops`.
