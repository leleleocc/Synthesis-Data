#!/bin/bash
# No-op in the sealed base. Phase 04 candidate overlays may replace this file
# so per-task fixtures, patches, or extra packages can land without rewriting
# the Dockerfile.
set -euo pipefail
exit 0
