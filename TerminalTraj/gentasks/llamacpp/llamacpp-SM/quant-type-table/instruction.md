Audit the llama.cpp quantization type table in `/app/src/llama-quant.cpp` and `/app/include/llama.h` and publish both a machine-readable mapping and a small probe binary.

Write `/results/quant_types.csv` as UTF-8, LF line endings, trailing newline, header exactly `ftype_enum,ftype_value,cli_name,default_ggml_type,default_ggml_type_id,imatrix_required`. One data row per `llama_ftype` enumerator in `/app/include/llama.h` that has a `case` in `llama_ftype_get_default_type` (skip `LLAMA_FTYPE_GUESSED` and commented-out enumerators), in the order those enumerators appear in `/app/include/llama.h`. `ftype_enum` is the full enumerator name. `ftype_value` is its integer value. `cli_name` is the matching `QUANT_OPTIONS` name from `/app/tools/quantize/quantize.cpp` when one exists; if several CLI names share the ftype, use the first in that table; if none, use an empty field. `default_ggml_type` is the `GGML_TYPE_*` suffix returned by `llama_ftype_get_default_type` (for example `Q4_K`). `default_ggml_type_id` is the integer `ggml_type` value from `/app/ggml/include/ggml.h`. `imatrix_required` is `1` if that default type is one of IQ3_XXS, IQ2_XXS, IQ2_XS, IQ2_S, IQ1_M, IQ1_S, or is Q2_K when the ftype is `LLAMA_FTYPE_MOSTLY_Q2_K_S`, else `0`. Do not emit a row for `COPY`.

Also build `/results/ftype_default` with `g++` (no extra libraries). It reads one integer ftype value from stdin, writes the default ggml type suffix and a newline, and exits 2 on an ftype that `llama_ftype_get_default_type` would reject (`GGML_TYPE_COUNT`). Do not modify files under `/data`.
