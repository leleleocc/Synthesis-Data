Reproduce a tokenizer fixture report for the Llama BPE vocab at `/app/models/ggml-vocab-llama-bpe.gguf` by reading that GGUF plus the tokenizer test pairing used by `/app/tests/test-tokenizer-0.cpp`, and report how much CPU memory those vocab tables occupy.

The test binary loads `<vocab-file>` and pairs it with `<vocab-file>.inp` and `<vocab-file>.out`. Parse those three paths. Treat GGUF as little-endian version 3 (magic `GGUF`). Report `tokenizer.ggml.model`, `tokenizer.ggml.pre`, `tokenizer.ggml.bos_token_id`, `tokenizer.ggml.eos_token_id`, the length of `tokenizer.ggml.tokens`, and the length of `tokenizer.ggml.merges`. Split the `.inp` file with the same case separator the test source uses. Token-id lines in `.out` are space-separated signed integers (leading spaces allowed); strip each `.out` line before parsing. A stripped empty `.out` line is an empty token list. Case `i` in `.inp` matches line `i` of `.out` (0-based). `input_bytes` is the UTF-8 byte length of the raw `.inp` case, not a Unicode character count.

Write `/results/tokenizer_report.json` as UTF-8 JSON with a trailing newline and keys in this order: `architecture` (string, `general.architecture`), `name` (string, `general.name`), `gguf_version` (int), `n_tensors` (int), `tokenizer_model` (string), `tokenizer_pre` (string), `vocab_size` (int, length of `tokenizer.ggml.tokens`), `n_merges` (int), `bos_token_id` (int), `eos_token_id` (int), `n_cases` (int), `cases` (array of objects, one per case in 0-based index order, keys in order `index`, `input_bytes`, `n_tokens`, `first_token`, `last_token`, `token_sum`; `first_token` and `last_token` are 0 when `n_tokens` is 0; `token_sum` is the integer sum of that case's token ids), `tokens_payload_bytes` (int, sum over every tokens-array string of 8 + byte length, the GGUF string encoding), `merges_payload_bytes` (int, same encoding for the merges array). Also write `/results/summarize_vocab.py` (Python 3, stdlib only). Invoked as `python3 /results/summarize_vocab.py /app/models/ggml-vocab-llama-bpe.gguf`, it prints a one-line JSON object with keys `tokenizer_model`, `vocab_size`, `n_merges` in that order (same values as the report) and a trailing newline. Do not modify files under `/data` or the files under `/app/models`.
