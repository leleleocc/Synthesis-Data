A CUDA-oriented cmake run left a poisoned cache and generator mix under `/data/incident/`. Restore a CPU-only Ninja configure of llama.cpp from `/app/CMakeLists.txt` at `/app/build` without dropping ggml CPU backend kernel options. A full compile of all targets is not required.

`/app/build` must be a CMake-generated Ninja tree: `/app/build/build.ninja` and `/app/build/CMakeCache.txt` must exist and must have been written by cmake (the cache must contain `CMAKE_CACHEFILE_DIR` and `CMAKE_GENERATOR:INTERNAL=Ninja`). Configure from `/app` so that `GGML_CPU` is ON and `GGML_CUDA`, `GGML_HIP`, `GGML_METAL`, `GGML_VULKAN`, `GGML_SYCL`, and `GGML_BLAS` are OFF. Keep ggml's host CPU backend: `GGML_NATIVE` ON and `GGML_CPU_REPACK` ON. Leave `GGML_BACKEND_DL` and `GGML_CPU_ALL_VARIANTS` OFF. Do not copy the incident compiler paths; `CMAKE_C_COMPILER` and `CMAKE_CXX_COMPILER` must resolve to the image gcc/g++ (gcc-14/g++-14). `CMAKE_BUILD_TYPE` must be `Release`.

After configure, `ninja -C /app/build -t targets` must list `ggml-cpu`. `ninja -C /app/build -t commands ggml-cpu` must print at least one compile or link command. Do not enable a missing GPU toolchain to "fix" the incident cache. Do not modify files under `/data`.
