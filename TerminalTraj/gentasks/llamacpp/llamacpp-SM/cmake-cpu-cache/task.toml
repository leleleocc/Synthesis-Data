schema_version = "1.4"

[task]
name = "terminaltraj/cmake-cpu-cache"
description = "Restore a poisoned CUDA CMake cache into a CPU-only Ninja configure that keeps ggml CPU backend kernel options."
authors = [{ name = "terminaltraj" }]
keywords = ["ml", "systems", "kernels", "debugging", "repair", "cmake", "ggml-cpu", "ninja"]

[metadata]
candidate_id = "c1"
package_slug = "cmake-cpu-cache"
difficulty = "hard"
category = "Software"
subcategory = "Systems"
tags = ["debugging", "repair", "cmake"]
domains = ["ml", "software"]
subdomains = ["systems", "kernels"]
languages = ["cpp", "c"]
oracle_types = ["state", "behavior"]
expert_time_estimate_min = 240
junior_time_estimate_min = 480

[agent]
timeout_sec = 14400

[verifier]
timeout_sec = 300

[environment]
network_mode = "public"
build_timeout_sec = 900
cpus = 4
memory_mb = 8192
storage_mb = 10240
workdir = "/app"
