### 理想pipeline：

1-了解目标仓库和target-spec，初始profile和过滤
2-build 初始base env，写一个合约json
3-Hierarchical Taxonomy根据target-spec生成详细的场景metadata，将spec转换为具体的slot
4-根据metadata设计任务的instruction与对base env的overlay，并作rebuild验证
5-设计verifier与task.toml
6-打包验证,安装harbor cli工具和python，进行包括TaskPaths.is_valid() TaskConfig.model_validate_toml()的简单验证，若验证出现问题及时修复
7-review任务，、harbor run -a nop、https://github.com/harbor-framework/terminal-bench/blob/main/docs/TASK_REVIEW_AUTOMATION.md中的Static Checks、Implementation Rubric Review

除非环境有问题，修改环境后instruction与环境冲突了，否则只改env和tests


### 各step我关注的点
1、base env要足够base
2、Hierarchical Taxonomy和spec设计的足够好，diversity、scalable、explainable
3、sepc的信息在03阶段之后全部变为slot中，一个slot应当是harbor任务信息最全的preview json版本，还需要包括下列内容
  - Single or multiple container
  - Separate verifier or not
  - Single step or multiple steps
  - Gpu task or not
  - Mcp task or not
4、instruction表述是否符合TB质量标准？
5、verifier的语言或者形式是否不单一？
6、打包之后，使用harbor库验证任务结构
7、在最后review阶段可以采用TaskPaths.is_valid() TaskConfig.model_validate_toml()检验harbor格式，没有harbor包就装（必须校验）
使用https://github.com/harbor-framework/terminal-bench/blob/main/docs/TASK_REVIEW_AUTOMATION.md中的Static Checks、Implementation Rubric Review，其他的检验暂时不进行，这里有一个问题就是design的时候需不需要暴露后面的质检要求？
8、harbor-task-creator是比较完整的skill，但是每一个阶段可能只需要用到其中的一部分


### 各文件设计
instruction：
题目设计要足够难，杂交多种failure_mode，不必要的信息在instruction不需要披露（比如环境里由agent可以自行探索的一些东西）

上面两个目标，具体的途径我能想到的就是挖bug（env=overlay里）、issue、pr、组合skill、或者让llm自己发散

toml:

默认single-step任务配置
build_time_out 600s
agent_timeout 7200s
verifier_timeout 600s

资源max限制(4cpu，8G内存、10G磁盘)

agent和env必须都是public，verifier可以为no

修复了skill和reference里对于 daytona gpu过时的描述

### todo
failure mode挖掘并融去产线

- subagent试运行，让主agent看思路，agent in the loop


披漏task-proposal
solution生成+ rollout loop 探讨
reasoning-effort
