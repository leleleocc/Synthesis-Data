schema_version = "1.4"

[task]
name = "terminaltraj/repair-http-rules"
description = "Repair HTTP event rules and Eve logging so TLS handshake metadata survives alongside HTTP alerts."
authors = [{ name = "terminaltraj", email = "terminaltraj@example.com" }]
keywords = ["security", "network-security", "http", "tls", "alerting", "log-analysis", "protocol", "suricata"]

[metadata]
candidate_id = "c3"
package_slug = "repair-http-rules"
difficulty = "hard"
category = "Security"
subcategory = "network-security"
tags = ["protocol", "http", "tls", "alerting", "log-analysis"]
domains = ["security", "software"]
subdomains = ["network-security", "forensics"]
languages = ["c", "python"]
oracle_types = ["state", "result"]
expert_time_estimate_min = 270
junior_time_estimate_min = 540

[agent]
timeout_sec = 16200

[verifier]
timeout_sec = 900

[environment]
network_mode = "public"
build_timeout_sec = 900
cpus = 4
memory_mb = 8192
storage_mb = 10240
workdir = "/app"
