schema_version = "1.4"

[task]
name = "terminaltraj/dns-name-decode"
description = "Reimplement Suricata DNS name decompression, including pointer, truncation, and label-limit cases, as a decoded corpus directory."
authors = [{ name = "terminaltraj" }]
keywords = ["dns", "suricata", "parsing", "forensics", "name-compression"]

[metadata]
candidate_id = "c3"
package_slug = "dns-name-decode"
difficulty = "hard"
category = "Security"
subcategory = "Forensics"
tags = ["dns", "protocol", "parsing", "network-security", "forensics", "debugging"]
domains = ["security", "software"]
subdomains = ["network-security", "forensics"]
languages = ["python", "shell"]
oracle_types = ["result", "state"]
expert_time_estimate_min = 195
junior_time_estimate_min = 400

[agent]
timeout_sec = 11700

[verifier]
timeout_sec = 300

[environment]
network_mode = "public"
build_timeout_sec = 600
cpus = 2
memory_mb = 4096
storage_mb = 8192
workdir = "/app"
