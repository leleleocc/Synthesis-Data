schema_version = "1.4"

[task]
name = "terminaltraj/cmake-pkg-cpu"
description = "Install llama.cpp as a relocatable CPU CMake package and prove find_package(Llama) with a probe binary."
authors = ["terminaltraj"]
keywords = ["software", "systems", "cmake", "build", "cpp", "linux"]

[metadata]
difficulty = "hard"
category = "Software"
subcategory = "Systems"
expert_time_estimate_min = 210
junior_time_estimate_min = 420
tags = ["build", "cmake", "cpp", "configuration", "linux"]
domains = ["ml", "software"]
subdomains = ["systems", "kernels"]
languages = ["cpp", "c", "shell"]
oracle_types = ["state", "behavior", "result"]

[agent]
timeout_sec = 12600

[verifier]
timeout_sec = 300

[environment]
network_mode = "public"
build_timeout_sec = 1800
cpus = 4
memory_mb = 8192
storage_mb = 16384
workdir = "/app"
