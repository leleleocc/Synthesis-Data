A conversion job left `/data/shard.gguf` in a state that llama.cpp's GGUF reader rejects. Repair it to `/results/repaired.gguf` so `gguf_init_from_file` succeeds with tensor data allocated. Keep every tensor name, dtype, shape, and payload byte sequence except for padding required by alignment. Do not modify files under `/data`.

The repaired file must be GGUF version 3, little-endian, with magic `GGUF`. Preserve every original key-value pair that is well typed; if a key appears more than once, keep the first occurrence and drop the rest. `general.alignment` must be stored as UINT32 with value 32. Align the start of the tensor data blob and each tensor `offset` to that value with zero padding. Write `/results/inventory.json` as UTF-8 JSON with keys in this order: `version` (int), `alignment` (int), `n_kv` (int), `n_tensors` (int), `kv` (array of `{key, type}` objects in file order; `type` is one of `UINT8`, `INT8`, `UINT16`, `INT16`, `UINT32`, `INT32`, `FLOAT32`, `BOOL`, `STRING`, `ARRAY`, `UINT64`, `INT64`, `FLOAT64`), `tensors` (array of `{name, n_dims, ne, type, nbytes, offset}` in file order; `ne` is an array of exactly 4 ints with unused dims set to 1; `type` is the uppercase ggml type name such as `F32` or `Q8_0`, not the lowercase `ggml_type_name` string; `nbytes` is the unpadded payload size in bytes; `offset` is the tensor data offset relative to the start of the data blob). End the file with a newline.

Also install `/results/bin/gguf-audit`, built from `/app` against ggml's public GGUF C API. It takes a single GGUF path argument, prints that same inventory JSON to stdout, and exits 0 when `gguf_init_from_file` returns a non-null context with tensor data allocated (`gguf_init_params.no_alloc` false and a non-null `ggml_context`).
