Bring up a local Redis 7 Sentinel cell that can fail over a master named `shop-master`. Build from `/app`. Do not modify files under `/src`. Bind everything to `127.0.0.1` with protected mode on and no requirepass.

Run one master on port `6379` and two replicas on ports `6380` and `6381` using disk-backed RDB (`dir` under `/results/sentinel-data/master`, `/results/sentinel-data/replica-6380`, `/results/sentinel-data/replica-6381` respectively, distinct `dbfilename`). Run three Sentinel processes on ports `26379`, `26380`, and `26381` with `quorum` 2, `down-after-milliseconds` 5000, `failover-timeout` 15000, and `parallel-syncs` 1. Point every Sentinel at `shop-master` on `127.0.0.1:6379`. Persist each Sentinel config under `/results/sentinel-data/sentinel-26379.conf` (and the 26380 / 26381 analogues) so a restart would still know the monitor.

Leave all six processes running. A replica INFO replication on 6380 and 6381 must show `role:slave` and `master_host` / `master_port` matching the current master. `SENTINEL get-master-addr-by-name shop-master` against any Sentinel must return `127.0.0.1` and `6379` while the original master is up. Write `/results/sentinel.json` as UTF-8 JSON with a trailing newline: `master_name` (`shop-master`), `master_addr` (array of two strings, ip then port), `num_other_sentinels` (integer from SENTINEL master, at least 2), `num_slaves` (integer, at least 2), and `sentinels` (array of the three Sentinel TCP ports as integers, sorted ascending).
