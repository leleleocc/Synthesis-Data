schema_version = "1.4"

[task]
name = "terminaltraj/sentinel-ha-cell"
description = "Run a Redis 7 master, two replicas, and a three-Sentinel quorum that can fail over shop-master."
authors = [{ name = "terminaltraj" }]
keywords = ["software", "systems", "redis", "sentinel", "replication", "configuration", "linux"]

[metadata]
candidate_id = "c4"
package_slug = "sentinel-ha-cell"
difficulty = "hard"
category = "Software"
subcategory = "Systems"
tags = ["configuration", "replication", "redis", "linux", "troubleshooting"]
domains = ["software"]
subdomains = ["systems", "databases"]
languages = ["c", "shell"]
oracle_types = ["result", "state", "behavior"]
expert_time_estimate_min = 300
junior_time_estimate_min = 600

[agent]
timeout_sec = 18000

[verifier]
timeout_sec = 300

[environment]
network_mode = "public"
build_timeout_sec = 900
cpus = 2
memory_mb = 4096
storage_mb = 8192
workdir = "/app"
