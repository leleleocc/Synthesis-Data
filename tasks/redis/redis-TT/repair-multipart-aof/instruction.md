A Redis 7.2 cache node crashed while rewriting its multi-part append-only file. The surviving directory is `/data/aof-incident/appendonlydir`. Do not modify anything under `/data`. Copy the tree to `/results/recovered/appendonlydir` and repair the manifest plus base/incremental AOF files there so that a `redis-check-aof` binary built from `/app`, invoked on `/results/recovered/appendonlydir/appendonly.aof.manifest` without `--fix`, reports the multi-part AOF as valid, and so a Redis 7.2.16 server built from `/app` can load it.

Build `/app/src/redis-server` and `/app/src/redis-check-aof` if they are not already present. Write `/results/recovered/redis.conf` with `bind 127.0.0.1`, `port 6379`, `protected-mode yes`, `daemonize no`, `dir /results/recovered`, `appendonly yes`, `appendfilename appendonly.aof`, `appenddirname appendonlydir`, `aof-load-truncated yes`, `appendfsync no`, `save ""`, `pidfile /results/recovered/redis.pid`, and `logfile /results/recovered/redis.log`. Start that server and leave it running on `127.0.0.1:6379`.

Recover the original dataset from the AOF rather than inserting a fresh keyspace. After load, string key `incident:ticket` must be `AOF-7721`, string key `incident:owner` must be `ops-oncall`, and hash `incident:meta` must contain field `host`=`cache-a` and field `zone`=`eu-west`. Then run `BGREWRITEAOF` and wait until the rewrite finishes so `/results/recovered/appendonlydir` holds a consistent multi-part AOF (manifest plus base and incremental files) that `redis-check-aof` reports as valid.

Write `/results/aof-report.json` as a single UTF-8 JSON object with no BOM, Unix newlines, and a trailing newline. Use exactly these keys in this order: `check_aof_ok`, `role`, `aof_enabled`, `ticket`, `owner`, `meta_host`. Set `check_aof_ok` to boolean `true`, `role` to the string `master`, `aof_enabled` to the integer `1`, `ticket` to the string `AOF-7721`, `owner` to the string `ops-oncall`, and `meta_host` to the string `cache-a`.
