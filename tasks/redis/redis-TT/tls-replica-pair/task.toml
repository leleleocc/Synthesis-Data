schema_version = "1.4"

[task]
name = "terminaltraj/tls-replica-pair"
description = "Build Redis with built-in TLS and run a TLS-only master/replica pair with split app and replication ACLs."
authors = [{ name = "terminaltraj" }]
keywords = ["software", "systems", "redis", "tls", "replication", "configuration", "build", "acl"]

[metadata]
candidate_id = "c2"
package_slug = "tls-replica-pair"
difficulty = "hard"
category = "Software"
subcategory = "Systems"
tags = ["build", "configuration", "replication", "redis", "linux"]
domains = ["software"]
subdomains = ["systems", "databases"]
languages = ["c", "shell"]
oracle_types = ["result", "state", "behavior"]
expert_time_estimate_min = 270
junior_time_estimate_min = 540

[agent]
timeout_sec = 16200

[verifier]
timeout_sec = 300

[environment]
network_mode = "public"
build_timeout_sec = 900
cpus = 2
memory_mb = 4096
storage_mb = 8192
workdir = "/app"
