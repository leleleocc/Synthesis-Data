A cache tier is losing replica RAM because replicas ignore `maxmemory` and never evict. Build Redis 7.2.16 from `/app` (default allocator) so `/app/src/redis-server` and `/app/src/redis-cli` exist. Write `/results/master.conf` and `/results/replica.conf` and start two processes that stay running.

The master listens on `127.0.0.1:6379` with `protected-mode yes`, `daemonize no`, `dir /results/master`, `dbfilename dump.rdb`, `pidfile /results/master.pid`, `logfile /results/master.log`, `save ""`, `appendonly no`, `maxmemory 2097152`, `maxmemory-policy allkeys-lru`, `maxmemory-samples 5`, and `min-replicas-to-write 1` with `min-replicas-max-lag 10`. The replica listens on `127.0.0.1:6380`, uses `replicaof 127.0.0.1 6379`, `replica-read-only yes`, `replica-serve-stale-data no`, `replica-ignore-maxmemory no`, `maxmemory 1048576`, `maxmemory-policy allkeys-lru`, `dir /results/replica`, `dbfilename dump.rdb`, `pidfile /results/replica.pid`, `logfile /results/replica.log`, `save ""`, `appendonly no`, and `daemonize no`. Create both `dir` paths first.

After the replica link is `up`, the replica must honor its own 1 MiB cap (it must not ignore maxmemory) while the master uses a 2 MiB cap and `allkeys-lru`. Writes on the master must succeed only when at least one replica is connected with replication lag no greater than `min-replicas-max-lag`. Leave both servers running.

Write `/results/cache-report.json` as a single UTF-8 JSON object with no BOM, Unix newlines, and a trailing newline. Use exactly these keys in this order: `master_maxmemory`, `replica_maxmemory`, `replica_ignore_maxmemory`, `master_policy`, `replica_policy`, `min_replicas_to_write`, `replica_role`, `master_link_status`. Set `master_maxmemory` to the integer `2097152`, `replica_maxmemory` to the integer `1048576`, `replica_ignore_maxmemory` to the string `no`, `master_policy` and `replica_policy` to the string `allkeys-lru`, `min_replicas_to_write` to the integer `1`, `replica_role` to the string `slave`, and `master_link_status` to the string `up`.
