Ops is migrating four historical RDB snapshots onto Redis 7.2 at `/app` and needs a conversion report before the cutover. The files are `/data/rdb/hash-zipmap.rdb`, `/data/rdb/hash-ziplist.rdb`, `/data/rdb/zset-ziplist.rdb`, and `/data/rdb/list-quicklist.rdb`. Build the server from `/app`. Do not modify files under `/src` or the RDB files under `/data`.

For each snapshot, load it as `dbfilename` with `dir` pointing at a private working directory that contains only a copy of that one file. Use a TCP port you choose, `bind 127.0.0.1`, and no password. Record `OBJECT ENCODING` for the payload key that actually lives in that file (`hash` in the two hash dumps, `zset` in the zset dump, `list` in the list dump). Do this twice per file: once with Redis 7.2 defaults, and once with the compact-encoding limit forced down so the loader cannot keep a compact encoding.

Write `/results/encodings.json` as UTF-8 JSON with a trailing newline. Top-level key `files` is an array of four objects in this order: `hash-zipmap.rdb`, `hash-ziplist.rdb`, `zset-ziplist.rdb`, `list-quicklist.rdb`. Each object has `file` (basename), `key` (the payload key name), `type` (Redis TYPE), `default_encoding` (OBJECT ENCODING string under defaults), `tight_encoding` (OBJECT ENCODING string with the compact-encoding limit forced down), and `fields` (hash: object of field to value; zset: array of `{"member":"...","score":<number>}` sorted by member; list: array of elements in list order). For hashes use `hash-max-listpack-entries` (alias `hash-max-ziplist-entries`) set to 1. For zsets use `zset-max-listpack-entries` (alias `zset-max-ziplist-entries`) set to 1. For the list dump use `list-max-listpack-size` set to 1. Scores are JSON numbers. Do not change the original files under `/data/rdb`.

Leave a Redis process running on `127.0.0.1:6379` with no password, `dir` a private copy of `hash-ziplist.rdb` loaded as `dbfilename`, and default encoding limits, so `OBJECT ENCODING hash` still matches `default_encoding` for that file.
