schema_version = "1.4"

[task]
name = "terminaltraj/rdb-encoding-audit"
description = "Load legacy zipmap, ziplist, and quicklist RDB snapshots under default and tight limits and report OBJECT ENCODING plus payload."
authors = [{ name = "terminaltraj" }]
keywords = ["software", "databases", "redis", "rdb", "cli", "troubleshooting", "encoding"]

[metadata]
candidate_id = "c3"
package_slug = "rdb-encoding-audit"
difficulty = "hard"
category = "Software"
subcategory = "Databases"
tags = ["cli", "troubleshooting", "redis", "debugging"]
domains = ["software"]
subdomains = ["systems", "databases"]
languages = ["c", "shell"]
oracle_types = ["result", "state", "behavior"]
expert_time_estimate_min = 210
junior_time_estimate_min = 420

[agent]
timeout_sec = 12600

[verifier]
timeout_sec = 180

[environment]
network_mode = "public"
build_timeout_sec = 900
cpus = 2
memory_mb = 4096
storage_mb = 8192
workdir = "/app"
