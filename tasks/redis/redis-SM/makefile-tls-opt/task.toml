schema_version = "1.4"

[task]
name = "terminaltraj/makefile-tls-opt"
description = "Migrate the Redis Makefile to default -O2 without LTO while keeping BUILD_TLS builtin and module paths working."
authors = [{ name = "terminaltraj" }]
keywords = ["software", "systems", "build", "build-system", "makefile", "optimization", "tls"]

[metadata]
candidate_id = "c3"
package_slug = "makefile-tls-opt"
difficulty = "hard"
category = "Software"
subcategory = "Systems"
tags = ["build", "build-system", "makefile", "optimization"]
domains = ["software"]
subdomains = ["systems", "databases"]
languages = ["c", "shell"]
oracle_types = ["result", "behavior"]
expert_time_estimate_min = 240
junior_time_estimate_min = 480

[agent]
timeout_sec = 14400.0

[verifier]
timeout_sec = 600.0

[environment]
network_mode = "public"
build_timeout_sec = 1800.0
cpus = 2
memory_mb = 4096
storage_mb = 8192
workdir = "/app"
