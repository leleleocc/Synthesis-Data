Produce an onboarding dry-run of the Redis build starting from `/app/Makefile` (the top-level wrapper that forwards into `/app/src/Makefile`) and extract how optimization flags, LTO, and the allocator are composed. Do not compile object files for this task; do not modify `/app/src/Makefile`, `/src`, or `/data`.

From `/app`, run a recursive no-op dry-run of the default `all` target that still shows the real compiler recipes rather than the quiet `CC` banners, and save the complete combined stdout+stderr in `/results/makefile-dry-run.log` (UTF-8, trailing newline). The log must include the inner `src` make invocation and at least one `gcc` recipe line for a Redis object.

Write `/results/opt-plan.json` as UTF-8 JSON with a trailing newline and exactly these keys in this order: `default_optimization` (string; the `OPTIMIZATION` default in `/app/src/Makefile`), `linux_default_malloc` (string; `jemalloc` or `libc` as selected when `uname -s` is Linux), `lto_cflag_gcc_o3` (string; the `REDIS_CFLAGS` addition used for GCC when optimization is `-O3`), `lto_ldflag` (string; the `REDIS_LDFLAGS` addition in that same `-O3` branch), `redis_cflags_vs_cflags` (string; `redis-only` if `REDIS_CFLAGS` is documented as applying to Redis objects and not to deps, otherwise `propagated-to-deps`), `jemalloc_in_final_libs` (boolean; whether a jemalloc static library is prepended to `FINAL_LIBS` when `MALLOC` is jemalloc), `persist_settings_file` (string; the filename `persist-settings` appends to in `src`). Take each value from the Makefiles or the dry-run log. After you finish, `make -n all` from `/app` must still succeed.
