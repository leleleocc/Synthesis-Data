Repair the RDB checksum path in `/app/src/rdb.c` so that save and load again feed CRC64 through the rio checksum callback when rdb-checksum is enabled, then audit the on-disk contract a later `redis-check-rdb` binary must honor. Use `/app/Makefile` to rebuild `/app/src/redis-server` and `/app/src/redis-check-rdb` from the repaired sources. Do not modify files under `/src` or `/data`.

Write `/results/rdb-audit.json` as UTF-8 JSON with a trailing newline and exactly these top-level keys in this order: `rdb_version` (integer; the current RDB format version written into the 9-byte magic), `magic_prefix` (string; the ASCII magic letters before the four version digits, REDIS), `eof_opcode` (integer), `aux_opcode` (integer), `selectdb_opcode` (integer), `resizedb_opcode` (integer), `expiretime_ms_opcode` (integer), `checksum_min_rdbver` (integer; oldest RDB version that stores a trailing CRC64), `zero_checksum_skips_validation` (boolean; whether a stored checksum of 0 means validation is skipped), `checksum_uses_rio_update` (boolean; whether save and load feed CRC64 through the rio checksum callback when rdb-checksum is enabled). Report the restored contract, not a damaged checksum path.

Also write `/results/rdb-check-help.txt` as the complete stdout+stderr captured from running the built `/app/src/redis-check-rdb` with no arguments (the process may exit non-zero). Leave no `redis-server` or `redis-check-rdb` process running.
