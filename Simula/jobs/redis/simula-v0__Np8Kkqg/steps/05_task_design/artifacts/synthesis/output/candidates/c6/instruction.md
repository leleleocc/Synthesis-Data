Rewrite `/app/redis.conf` into a standalone replica configuration that can reproduce a local master/replica pair for WAIT/WAITAOF-style testing, and write a matching master profile beside it. Leave every unrelated directive at its shipped default. Do not enable cluster, sentinel, or TLS. Do not modify files under `/src`.

The replica file `/app/redis.conf` must have these effective (last uncommented) values: `bind 127.0.0.1`, `port 6380`, `protected-mode yes`, `daemonize no`, `replicaof 127.0.0.1 6379`, `replica-read-only yes`, `replica-serve-stale-data no`, `repl-diskless-sync no`, `repl-backlog-size 16mb`, `appendonly yes`, `appendfsync always`, `no-appendfsync-on-rewrite no`, `dir /data/replica`, `dbfilename replica.rdb`, `pidfile /results/replica.pid`, `logfile /results/replica.log`. Create `/data/replica` and `/data/master` as empty writable directories.

Write `/results/master.conf` as a second Redis config (same comment style is fine) whose effective values are: `bind 127.0.0.1`, `port 6379`, `protected-mode yes`, `daemonize no`, `min-replicas-to-write 1`, `min-replicas-max-lag 10`, `repl-backlog-size 16mb`, `appendonly yes`, `appendfsync everysec`, `dir /data/master`, `dbfilename master.rdb`, `pidfile /results/master.pid`, `logfile /results/master.log`. Do not set `replicaof` on the master. Write `/results/replica-profile.json` as UTF-8 JSON with a trailing newline and exactly these keys in this order: `replica_port` (integer; 6380), `master_port` (integer; 6379), `replicaof` (string; `127.0.0.1 6379`), `replica_read_only` (string; `yes`), `replica_serve_stale_data` (string; `no`), `replica_appendfsync` (string; `always`), `master_min_replicas_to_write` (integer; 1), `master_min_replicas_max_lag` (integer; 10), `repl_backlog_size` (string; `16mb`), `diskless_sync` (string; `no`). Leave no Redis process running.
