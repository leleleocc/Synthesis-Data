Build Redis from `/app` and use `/app/runtest` to run the WAIT/WAITAOF unit tests that configure AOF fsync policy and replica links at runtime. Keep `/app/redis.conf` compatible with the Tcl harness: do not add `replicaof`, do not set `port 0`, do not enable cluster or TLS, and do not change `dir` away from the test helper's working directories. Do not modify files under `/src` or `/data`.

Compile `/app/src/redis-server`, `/app/src/redis-check-aof`, `/app/src/redis-cli`, and `/app/src/redis-benchmark` (the binaries `runtest` expects). Then from `/app` invoke `/app/runtest --single unit/wait --clients 1` and capture the complete combined stdout+stderr in `/results/runtest.log` (UTF-8, trailing newline). The run must finish with the suite's success banner (`All tests passed without errors!`) and exit status 0; do not pass `--tags` that drop `needs:repl` tests, and do not skip `unit/wait`.

Write `/results/unit-test-report.json` as UTF-8 JSON with a trailing newline and exactly these keys in this order: `unit` (string; `unit/wait`), `clients` (integer; `1`), `exit_code` (integer; the `runtest` process exit code), `passed` (boolean; true only when exit code is 0 and the log contains the success banner), `log_path` (string; `/results/runtest.log`), `replication_tests_included` (boolean; true when the log shows WAIT/WAITAOF replica cases ran rather than being filtered out). When you stop, `/app/src/redis-server` must still exist as an ELF binary, `/results/runtest.log` must exist, and no `redis-server` process may remain.
