Add a `help` target so `make -C /app help` reports the compile knobs this tree actually uses: `OPTIMIZATION`, `MALLOC`, `BUILD_TLS`, and `PREFIX`. Printed values must come from the Make variables that compile Redis, including `/app/src/.make-settings` when that file exists, and must change if the caller overrides `BUILD_TLS`, `OPTIMIZATION`, or `MALLOC` on the make command line. Default `make -C /app help` (no extra variables) must still succeed, `make all` must still build `/app/src/redis-server` and `/app/src/redis-cli`, and the help text must mention the `BUILD_TLS=yes` and `BUILD_TLS=module` recipes.

Write that default help output to `/results/make-help.log` as UTF-8 (ASCII subset) with a trailing newline. The log must contain `OPTIMIZATION=...`, `MALLOC=...`, `BUILD_TLS=...`, and `PREFIX=...` lines (current effective values) plus at least one line containing `BUILD_TLS=yes`. Do not install into `/usr/local`.

Start `/app/src/redis-server` on `127.0.0.1:6379` with the in-tree binary and, using `/app/src/redis-cli`, write `/results/runtime-build.env` as ASCII `KEY=value` lines with a trailing newline and keys in this order: `REDIS_VERSION`, `TCP_PORT`, `EXECUTABLE`, `GCC_VERSION`, `MALLOC`. Values must match live `INFO server` fields `redis_version`, `tcp_port`, `executable`, `gcc_version`, and `mem_allocator`. Leave the server running and not daemonized.
