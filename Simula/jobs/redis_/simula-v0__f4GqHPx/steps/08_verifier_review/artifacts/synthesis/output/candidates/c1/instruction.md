Repair the AOF rewrite path in `/app/src/aof.c` so a master can rewrite its append-only file without dropping replication identity. After `BGREWRITEAOF` the instance must keep a stable replication ID, must not rewind `master_repl_offset`, and must keep a usable fsynced replication offset so `WAITAOF` can still wait for local AOF fsync. The rewritten AOF base (RDB preamble) must carry the same replication ID and offset that `INFO replication` reports. The `/app/src/redis-server` binaries that remain running must be produced from that repaired source.

Leave a repaired master running at `127.0.0.1:6379` with `appendonly yes`, `appendfsync everysec`, `dir /data`, `dbfilename dump.rdb`, and a replica at `127.0.0.1:6380` that stays in sync after the rewrite (PSYNC, not a full resync forced by a zeroed offset). Do not change files under `/opt/env`. The dataset after rewrite must still contain string key `incident:ticket` with value `AOF-42` and must accept a further `INCR incident:seq` without breaking the replica offset.

When you stop, `/app/src/redis-server` must still be running on 6379, `/app/src/redis-server` on 6380 must still be a replica of it, `/data/appendonlydir` must contain a rewritten base file plus manifest, and `/results/aof_repair.env` must exist as ASCII `KEY=value` lines with a trailing newline and these keys in this order: `MASTER_REPLID`, `MASTER_REPL_OFFSET`, `ROLE`, `AOF_ENABLED`, `REPLICA_REPL_OFFSET`. `MASTER_REPLID`, `MASTER_REPL_OFFSET`, and `ROLE` must equal the master's live `INFO replication` fields `master_replid`, `master_repl_offset`, and `role`. `AOF_ENABLED` must equal the master's live `INFO persistence` field `aof_enabled`. `REPLICA_REPL_OFFSET` must equal the replica's live `INFO replication` field `slave_repl_offset`. Do not daemonize with a pid file under `/var/run`.
