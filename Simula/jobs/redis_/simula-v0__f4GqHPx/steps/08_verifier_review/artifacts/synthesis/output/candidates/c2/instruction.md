Audit and repair RDB load integrity in `/app/src/rdb.c` and `/app/src/redis-check-rdb.c`. `/data/dump.rdb` is a valid Redis 7 dump from this tree, but the current binaries reject it or load expires with the wrong remaining TTL. Fix the loader so `/app/src/redis-server` and `/app/src/redis-check-rdb` both accept that file, restore expires with the original millisecond precision, and still reject a truncated copy. The running `/app/src/redis-server` and `/app/src/redis-check-rdb` must be produced from that repaired source.

Start `/app/src/redis-server` on `127.0.0.1:6379` with `dir /data` and `dbfilename dump.rdb` so it loads that dump. Do not modify `/data/dump.rdb`. After load, `user:1` must be string `alice` with a remaining TTL greater than 86400 seconds (the dump stored a multi-day millisecond expire; a seconds-scale leftover is wrong), `cache:flag` must be string `ok` with no expire, and `stream:events` must be a stream whose last entry ID is `1700000000000-0` with field `status` equal to `ok`. Do not rewrite the keys to fake a pass.

Leave the server process running (not daemonized). Write `/results/rdb_audit.env` as ASCII `KEY=value` lines with a trailing newline and keys in this order: `CHECK_RDB_STATUS`, `KEYS_LOADED`, `USER1`, `USER1_TTL_GT_60`, `CACHE_FLAG`, `STREAM_LAST_ID`. `CHECK_RDB_STATUS` must be `ok` if `/app/src/redis-check-rdb /data/dump.rdb` exits 0, otherwise `err`. `KEYS_LOADED` must be the integer from `INFO keyspace` `keys=` on db0. `USER1` must be the live `GET user:1` value. `USER1_TTL_GT_60` must be `1` if the remaining TTL of `user:1` is greater than 86400 seconds, else `0`. `CACHE_FLAG` must be the live `GET cache:flag` value. `STREAM_LAST_ID` must be the live stream last entry ID (`1700000000000-0` when the dump loaded correctly).
