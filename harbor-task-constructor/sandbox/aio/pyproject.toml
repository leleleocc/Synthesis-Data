[project]
name = "harbor-aio-sandbox"
version = "0.1.0"
description = "AIO Sandbox execution path for the Harbor task constructor"
requires-python = ">=3.10"
dependencies = [
    "tos>=2.9.2",
    "volcengine-python-sdk>=5.0.48",
]

[tool.uv]
package = false
