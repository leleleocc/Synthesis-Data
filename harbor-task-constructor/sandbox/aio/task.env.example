# 业务方交过来的凭证。一行一个 KEY=VALUE。
#
# 复制成 sandbox/aio/task.env（或 sandbox/aio/tasks/<task-id>.env）并 chmod 600。
# 两个位置都已被 .gitignore 的 /sandbox/aio/*.env 和 /sandbox/aio/tasks/ 挡住。
#
# 这个文件**只在主机侧读一次**，变成 CreateSandboxRequest.envs，**本体不进沙箱**。
# 所以凭证从来没有以文件形式静止过：不在 TOS 上、不在 NAS 树里、不在任何归档包里。
# 归档是整棵树打包，凭证要是落在树里就会进每一个 checkpoint——一次泄漏变成几十份。
#
# 沙箱里跑的是 --dangerously-skip-permissions 的 agent，无人监督。
# **给专用的、有额度上限的、能单独吊销的 token，别用日常那个。**
#
# 下面是 claude -p 认的几个。按你的接入方式挑，用不到的留空即可：
#   - 官方 API：填 ANTHROPIC_API_KEY
#   - 网关 / 中转：填 ANTHROPIC_AUTH_TOKEN + ANTHROPIC_BASE_URL
# 除了这几个，任何构造过程要用的自己的密钥也可以直接加在这里，会一起注入。

ANTHROPIC_API_KEY=
ANTHROPIC_AUTH_TOKEN=
ANTHROPIC_BASE_URL=
ANTHROPIC_MODEL=
